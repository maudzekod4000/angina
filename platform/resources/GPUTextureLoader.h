#ifndef PLATFORM_RESOURCES_GPU_TEXTURE_LOADER_H
#define PLATFORM_RESOURCES_GPU_TEXTURE_LOADER_H

namespace Platform::Resources {
/// This should be an interface for GPU loading, but
/// I don't see much of a difference with the CPUTextureLoader interface...
/// There is quite a lot of duplication....
class GPUTextureLoader {
public:

};
}

#endif // !PLATFORM_RESOURCES_GPU_TEXTURE_LOADER_H
